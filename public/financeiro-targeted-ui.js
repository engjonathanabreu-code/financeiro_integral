/* Refinamento visual pontual: somente cards de proximos vencimentos/ERP e aba Planejamento. */
(function(){
'use strict';
const q=s=>document.querySelector(s), qa=s=>[...document.querySelectorAll(s)];
const E=v=>typeof esc==='function'?esc(String(v??'')):String(v??'');
const M=v=>typeof moneySafe==='function'?moneySafe(v):(typeof money==='function'?money(v):Number(v||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'}));
const F=d=>{if(!d)return'—';try{return typeof fmt==='function'?fmt(d):new Date(d+'T12:00:00').toLocaleDateString('pt-BR')}catch{return d}};
const monthOf=v=>String(v||'').slice(0,7);
const monthLabelLocal=m=>{if(typeof monthLabel==='function')return monthLabel(m);const[y,mo]=String(m).split('-');return new Date(+y,+mo-1,1).toLocaleDateString('pt-BR',{month:'long',year:'numeric'}).replace(/^./,x=>x.toUpperCase())};
const addMonthsLocal=(date,n)=>{const d=new Date(date+'T12:00:00');d.setDate(1);d.setMonth(d.getMonth()+n);return d.toISOString().slice(0,10)};
const today=()=>new Date().toISOString().slice(0,10);
const daysUntil=d=>Math.max(0,Math.ceil((new Date(d+'T12:00:00')-new Date(today()+'T12:00:00'))/86400000));
function getDB(){try{return db}catch{return window.db||{}}}
function isAdmin(){try{return !user||user.role==='Administrador'}catch{return true}}

function refinedDashboard(){
  if(!isAdmin())return typeof documents==='function'?documents():null;
  if(typeof title==='function')title('Visão Geral');
  const d=getDB(),c=q('#content');if(!c)return;
  const t=today(),masters=d.accountMasters||[];
  const payments=(d.accountPayments||[]).filter(x=>!['Paga','Pago','Cancelada','Cancelado'].includes(String(x.status||''))&&String(x.due||'')>=t).sort((a,b)=>String(a.due||'').localeCompare(String(b.due||''))).slice(0,8);
  const erp=(d.erpPlannedRevenues||[]).filter(x=>Number(x.remaining||0)>0&&String(x.dueDate||'')>=t).sort((a,b)=>String(a.dueDate||'').localeCompare(String(b.dueDate||''))).slice(0,8);
  const cash=d.cashflow||[],docs=[...(d.docs||[])].sort((a,b)=>String(b.date||b.createdAt||'').localeCompare(String(a.date||a.createdAt||''))).slice(0,6),trips=(d.trips||[]).filter(x=>!/finaliz|aprovad|conclu|encerrad/i.test(String(x.status||''))).slice(0,6);
  const val=x=>Number(x?.value??x?.amount??x?.valor??x?.remaining??0),balance=cash.reduce((s,x)=>s+(x.direction==='Entrada'?val(x):-val(x)),0),openAccounts=payments.reduce((s,x)=>s+val(x),0),expected=erp.reduce((s,x)=>s+Number(x.remaining||0),0);
  const todayDate=new Date(t+'T12:00:00'),limit60=new Date(todayDate);limit60.setDate(limit60.getDate()+60);const contractDue=(d.hrPeople||[]).filter(p=>p.end&&new Date(p.end+'T12:00:00')>=todayDate&&new Date(p.end+'T12:00:00')<=limit60).sort((a,b)=>String(a.end).localeCompare(String(b.end)));
  const accountRows=payments.map(p=>{const a=masters.find(x=>String(x.id)===String(p.accountId)),n=daysUntil(p.due);return `<div class="fin-row"><div class="fin-row-main"><b>${E(a?.name||a?.supplier||'Conta')}</b><div class="fin-row-meta"><span>${F(p.due)}</span><span class="fin-dot"></span><span>${E(p.status||'A vencer')}</span></div></div><div class="fin-row-value"><strong>${M(val(p))}</strong><small>${n===0?'Hoje':n===1?'1 dia':n+' dias'}</small></div></div>`}).join('')||'<div class="empty">Nenhuma conta futura pendente.</div>';
  const erpRows=erp.map(x=>`<div class="fin-row"><div class="fin-row-main"><b>${E(x.project||'Projeto ERP')}</b><div class="fin-row-meta"><span>${E(x.origin||'Recebimento')}</span><span class="fin-dot"></span><span>${F(x.dueDate)}</span></div></div><div class="fin-row-value"><strong>${M(x.remaining)}</strong>${x.installment?`<small>${E(x.installment)}</small>`:''}</div></div>`).join('')||'<div class="empty">Nenhuma previsão do ERP disponível.</div>';
  c.innerHTML=`<div class="grid cols-4"><div class="card metric"><h3>Saldo atual do caixa</h3><b>${M(balance)}</b><small>movimentações registradas</small></div><div class="card metric"><h3>Contas a vencer</h3><b>${M(openAccounts)}</b><small>${payments.length} lançamento(s) próximo(s)</small></div><div class="card metric"><h3>Entradas previstas ERP</h3><b>${M(expected)}</b><small>${erp.length} parcela(s) próxima(s)</small></div><div class="card metric"><h3>Documentos recentes</h3><b>${docs.length}</b><small>processados no Financeiro</small></div></div>${contractDue.length?`<section class="card" style="margin-top:16px"><div class="section-head"><div><h3>Contratos próximos do vencimento</h3><p class="muted">Contratos que vencem nos próximos 60 dias.</p></div><span class="badge warn">${contractDue.length}</span></div>${contractDue.map(p=>`<div class="mini-row readonly"><span><b>${E(p.name||'Colaborador')}</b><small>Vencimento em ${F(p.end)}</small></span><strong>${daysUntil(p.end)} dias</strong></div>`).join('')}</section>`:''}<div class="dashboard-finance-panels"><section class="card dashboard-finance-panel"><div class="fin-panel-head"><div class="fin-panel-title"><span class="fin-panel-icon">▣</span><div><h3>Próximas contas a vencer</h3><p class="muted">Pagamentos já cadastrados.</p></div></div></div><div class="fin-list">${accountRows}</div><div class="fin-panel-total"><span>Total dos próximos lançamentos</span><b>${M(openAccounts)}</b></div></section><section class="card dashboard-finance-panel"><div class="fin-panel-head"><div class="fin-panel-title"><span class="fin-panel-icon">↗</span><div><h3>Próximas entradas do ERP</h3><p class="muted">Últimos dados sincronizados disponíveis.</p></div></div></div><div class="fin-list">${erpRows}</div><div class="fin-panel-total"><span>Total previsto</span><b>${M(expected)}</b></div></section></div><div class="grid cols-2" style="margin-top:16px"><section class="card"><div class="section-head"><div><h3>Últimos documentos fiscais</h3></div></div>${docs.map(x=>`<div class="mini-row readonly"><span><b>${E(x.name||'Documento')}</b><small>${E(x.supplier||'')} • ${F(x.date||x.createdAt)}</small></span><strong>${M(val(x))}</strong></div>`).join('')||'<div class="empty">Nenhum documento fiscal registrado.</div>'}</section><section class="card"><div class="section-head"><div><h3>Viagens ativas</h3></div></div>${trips.map(x=>`<div class="mini-row readonly"><span><b>${E(x.city||x.project||'Viagem')}</b><small>${E(x.employee||x.status||'')}</small></span><strong>${M(x.proven??x.spent??x.declared??0)}</strong></div>`).join('')||'<div class="empty">Nenhuma viagem ativa.</div>'}</section></div>`;
}

function refinedPlanning(){
  if(!isAdmin())return typeof documents==='function'?documents():null;
  if(typeof title==='function')title('Planejamento');
  if(typeof syncERPReceivables==='function')syncERPReceivables();
  const d=getDB(),start=monthOf(today()),months=Array.from({length:12},(_,i)=>monthOf(addMonthsLocal(start+'-01',i))),rows=typeof window.integralPlanningRows==='function'?window.integralPlanningRows():[];
  const real=(d.cashflow||[]).map(x=>({...x,month:monthOf(x.date)}));
  const totals=months.reduce((acc,m)=>{const a=rows.filter(x=>x.month===m),iv=a.filter(x=>x.direction==='Entrada').reduce((s,x)=>s+Number(x.value||0),0),ov=a.filter(x=>x.direction==='Saída').reduce((s,x)=>s+Number(x.value||0),0);acc.in+=iv;acc.out+=ov;return acc},{in:0,out:0});
  const monthCards=months.map((m,i)=>{const a=rows.filter(x=>x.month===m),iv=a.filter(x=>x.direction==='Entrada').reduce((s,x)=>s+Number(x.value||0),0),ov=a.filter(x=>x.direction==='Saída').reduce((s,x)=>s+Number(x.value||0),0),accounts=a.filter(x=>x.source==='Contas').reduce((s,x)=>s+Number(x.value||0),0),payroll=a.filter(x=>x.source==='RH').reduce((s,x)=>s+Number(x.value||0),0),bal=iv-ov;return `<button class="card planning-month-card" data-refined-month="${m}"><div class="planning-month-title"><strong>${E(monthLabelLocal(m))}</strong>${i===0?'<span>Atual</span>':''}</div><div class="planning-month-lines"><div class="planning-month-line"><span>Entradas</span><b class="kpi-positive">${M(iv)}</b></div><div class="planning-month-line"><span>Saídas</span><b class="kpi-negative">${M(ov)}</b></div><div class="planning-month-line"><span>Contas</span><b>${M(accounts)}</b></div><div class="planning-month-line"><span>Colaboradores</span><b>${M(payroll)}</b></div></div><div class="planning-month-balance"><span>Saldo previsto</span><b class="${bal>=0?'kpi-positive':'kpi-negative'}">${M(bal)}</b></div></button>`}).join('');
  q('#content').innerHTML=`<div class="planning-refined"><div class="planning-refined-head"><div><h3>Planejamento dos próximos 12 meses</h3><p class="muted">Contas cadastradas, custos de colaboradores e entradas do ERP são incorporados automaticamente ao mês correspondente. O planejamento não altera o Fluxo de Caixa.</p></div><div class="right"><button class="btn ghost" id="refinedPlanExpense">+ Despesa planejada</button><button class="btn" id="refinedPlanRevenue">+ Receita planejada</button></div></div><div class="planning-summary-grid"><div class="card planning-summary-card"><span>Entradas previstas</span><b class="kpi-positive">${M(totals.in)}</b><small>próximos 12 meses</small></div><div class="card planning-summary-card"><span>Saídas previstas</span><b class="kpi-negative">${M(totals.out)}</b><small>contas, RH e despesas manuais</small></div><div class="card planning-summary-card"><span>Resultado previsto</span><b class="${totals.in-totals.out>=0?'kpi-positive':'kpi-negative'}">${M(totals.in-totals.out)}</b><small>saldo projetado acumulado</small></div></div><div class="planning-month-grid">${monthCards}</div><div class="planning-detail-grid"><section class="card planning-source-card"><div class="section-head"><h3>Receitas planejadas manuais</h3></div>${(d.planRevenues||[]).map(r=>`<button class="mini-row" data-refined-revenue="${r.id}"><span><b>${E(r.origin)}</b><small>${r.installments||1} parcela(s) • ${E(r.cadence||'Mensal')}</small></span><strong>${M(r.total)}</strong></button>`).join('')||'<div class="empty">Nenhuma receita manual cadastrada.</div>'}</section><section class="card planning-source-card"><div class="section-head"><h3>Despesas planejadas manuais</h3></div>${(d.planExpenses||[]).map(r=>`<button class="mini-row" data-refined-expense="${r.id}"><span><b>${E(r.origin||r.name||'Despesa')}</b><small>${r.installments||1} ocorrência(s) • ${E(r.cadence||'Mensal')}</small></span><strong>${M(r.total||r.value)}</strong></button>`).join('')||'<div class="empty">Nenhuma despesa manual cadastrada.</div>'}</section></div><div class="planning-automatic-grid"><section class="card planning-auto-card"><h3>Contas automáticas</h3><p>Os pagamentos cadastrados em Contas entram no mês do vencimento.</p></section><section class="card planning-auto-card"><h3>Custos de colaboradores</h3><p>O valor vigente do RH é projetado automaticamente durante a vigência do contrato.</p></section></div></div>`;
  q('#refinedPlanRevenue').onclick=()=>typeof revenueForm==='function'&&revenueForm();q('#refinedPlanExpense').onclick=()=>typeof expensePlanForm==='function'&&expensePlanForm();qa('[data-refined-month]').forEach(b=>b.onclick=()=>typeof planningMonthDetail==='function'&&planningMonthDetail(b.dataset.refinedMonth));qa('[data-refined-revenue]').forEach(b=>b.onclick=()=>typeof revenueForm==='function'&&revenueForm(b.dataset.refinedRevenue));qa('[data-refined-expense]').forEach(b=>b.onclick=()=>typeof expensePlanForm==='function'&&expensePlanForm(b.dataset.refinedExpense));
}

try{dashboard=refinedDashboard;window.dashboard=refinedDashboard}catch{window.dashboard=refinedDashboard}
try{planning=refinedPlanning;window.planning=refinedPlanning}catch{window.planning=refinedPlanning}
window.renderFinanceDashboardFinal=refinedDashboard;
window.addEventListener('integral:erp-planning-synced',()=>{try{if((typeof view!=='undefined'?view:window.view)==='planning')refinedPlanning();else if((typeof view!=='undefined'?view:window.view)==='dashboard')refinedDashboard()}catch{}});
})();
